import { LegalPage } from "@/components/LegalPage";
export const dynamic = "force-dynamic";
export const metadata = { title: "Privacy Policy" };
export default function PrivacyPage() { return <LegalPage title="Privacy Policy" updated="15 August 2026"><h2>1. Scope</h2><p>This policy describes how the FlytheBG background-removal service handles information when you use the website.</p><h2>2. Image processing</h2><p>Uploaded images are processed for the purpose of producing the background-removed result. The current application architecture is designed for in-memory processing and does not intentionally save uploaded or processed images to a permanent application database or object store.</p><h2>3. Model training</h2><p>Uploaded images are not used to train background-removal models in this implementation.</p><h2>4. Technical data</h2><p>Infrastructure may process limited technical information such as request timing, status codes, error categories, and security-related events. Application logging must not include image binaries or private image URLs.</p><h2>5. Metadata</h2><p>Processed output is re-encoded as PNG. Unnecessary source metadata is not intentionally copied into the output.</p><h2>6. Security</h2><p>The service uses server-side file validation, bounded image sizes and pixel counts, private service communication where available, and request concurrency controls.</p><h2>7. Contact</h2><p>Use the contact details shown below for privacy questions. These details must be configured with real company information before public launch.</p></LegalPage>; }
