type Size = { size: number; x: number; y: number; width: number; height: number };
export type Detection = Size & { score: number; matchFound: boolean; gain: number };
export type WatermarkSettings = { gain?: number; scale?: number; offsetX?: number; offsetY?: number };
export type WatermarkPosition = { x: number; y: number; width: number; height: number };
export type WatermarkEngine = { getWatermarkInfo:(width:number,height:number)=>{size:number;position:WatermarkPosition}; getAlphaMap:(size:number)=>Promise<Float32Array>; removeWatermarkFromImage:(canvas:HTMLCanvasElement,settings?:WatermarkSettings)=>Promise<HTMLCanvasElement> };
const ALPHA_THRESHOLD=.002,MAX_ALPHA=.99,LOGO_VALUE=255;
const BG_48="data:image/png;base64,";
const clamp=(v:number,min:number,max:number)=>Math.max(min,Math.min(max,v));
function getWatermarkInfo(width:number,height:number):Size{const m=Math.min(width,height),r=m/1536,s=Math.max(16,Math.round(96*r)),margin=Math.max(8,Math.round(64*r));return{size:s,x:Math.max(0,width-margin-s),y:Math.max(0,height-margin-s),width:s,height:s};}
function getVeoWatermark(width:number,height:number):Size{const b=Math.min(width,height),s=Math.max(24,Math.min(Math.round(b/15),b)),m=Math.round(b/10);return{size:s,x:Math.max(0,width-m-s),y:Math.max(0,height-m-s),width:s,height:s};}
const imageCache=new Map<number,HTMLImageElement>();
async function getReference(size:number){const old=imageCache.get(size);if(old)return old;const img=await new Promise<HTMLImageElement>((res,rej)=>{const i=new Image();i.onload=()=>res(i);i.onerror=()=>rej(new Error('Gemini reference asset could not be loaded.'));i.src=BG_48;});imageCache.set(size,img);return img;}
async function alphaMap(size:number){const ref=await getReference(size),c=document.createElement('canvas');c.width=size;c.height=size;const x=c.getContext('2d',{willReadFrequently:true});if(!x)throw new Error('Canvas processing is unavailable.');x.imageSmoothingEnabled=true;x.imageSmoothingQuality='high';x.drawImage(ref,0,0,size,size);const d=x.getImageData(0,0,size,size).data,a=new Float32Array(size*size);for(let i=0;i<a.length;i++){const o=i*4;a[i]=Math.max(d[o],d[o+1],d[o+2])/255;}return a;}
function scoreCandidate(data:ImageData,width:number,height:number,ref:HTMLImageElement,box:{x:number;y:number;size:number}){const{x,y,size}=box;if(x<0||y<0||x+size>width||y+size>height)return-1;const c=document.createElement('canvas');c.width=size;c.height=size;const x=c.getContext('2d',{willReadFrequently:true});if(!x)return-1;x.drawImage(ref,0,0,size,size);const rd=x.getImageData(0,0,size,size).data;let sl=0,sa=0,sl2=0,sa2=0,sla=0,n=0;const step=size>80?2:1;for(let r=0;r<size;r+=step)for(let col=0;col<size;col+=step){const ii=((y+r)*width+x+col)*4,ri=(r*size+col)*4,l=.299*data.data[ii]+.587*data.data[ii+1]+.114*data.data[ii+2],a=Math.max(rd[ri],rd[ri+1],rd[ri+2])/255;sl+=l;sa+=a;sl2+=l*l;sa2+=a*a;sla+=l*a;n++;}if(!n)return-1;const ml=sl/n,ma=sa/n,vl=Math.max(0,sl2/n-ml*ml),va=Math.max(0,sa2/n-ma*ma);if(vl<.5||va<.0001)return 0;return Math.max(0,(sla/n-ml*ma)/Math.sqrt(vl*va));}
export async function detectGeminiWatermark(data:ImageData,width:number,height:number,mode:'image'|'video'='image'):Promise<Detection>{const ref=await getReference(48),base=mode==='video'?getVeoWatermark(width,height):getWatermarkInfo(width,height),dim=Math.min(width,height),layouts=mode==='video'?[{b:base.size,m:Math.round(dim/10),p:1.06},{b:Math.max(24,Math.round(96*(dim/1536))),m:Math.max(16,Math.round(192*(dim/1536))),p:1.01}]:[{b:base.size,m:Math.max(8,Math.round(192*(dim/1536))),p:1.08},{b:base.size,m:Math.max(8,Math.round(64*(dim/1536))),p:1.04},{b:96,m:dim>=1400?192:Math.round(128*Math.max(.5,dim/1024)),p:1.02}],scales=mode==='video'?[.65,.85,1,1.2,1.45]:[.55,.7,.85,1,1.15,1.3,1.5,1.7];let best={score:-1,x:base.x,y:base.y,size:base.size,p:1};for(const l of layouts)for(const z of scales){const size=Math.max(16,Math.min(Math.round(l.b*z),dim-8)),x=Math.max(0,width-l.m-size),y=Math.max(0,height-l.m-size),score=scoreCandidate(data,width,height,ref,{x,y,size})*l.p;if(score>best.score)best={score,x,y,size,p:l.p};}if(best.score>.05)for(const ds of [.9,.95,1,1.05,1.1]){const size=Math.max(16,Math.min(Math.round(best.size*ds),dim-8));for(let dy=-16;dy<=16;dy+=4)for(let dx=-16;dx<=16;dx+=4){const x=clamp(best.x+dx,0,width-size),y=clamp(best.y+dy,0,height-size),s=scoreCandidate(data,width,height,ref,{x,y,size})*best.p;if(s>best.score)best={score:s,x,y,size,p:best.p};}}const score=clamp(best.score,0,1);return{matchFound:score>=(mode==='video'?.08:.10),score,x:best.x,y:best.y,size:best.size,width:best.size,height:best.size,gain:.6};}
export async function removeGeminiWatermark(data:ImageData,d:Detection,settings:WatermarkSettings={}):Promise<ImageData>{if(!d.matchFound)return data;const gain=clamp(settings.gain??d.gain??.6,0,1),scale=clamp(settings.scale??1,.5,2),size=Math.max(16,Math.round(d.size*scale)),a=await alphaMap(size),x=clamp(Math.round(d.x+(d.width-size)/2+(settings.offsetX??0)),0,data.width-size),y=clamp(Math.round(d.y+(d.height-size)/2+(settings.offsetY??0)),0,data.height-size);for(let r=0;r<size;r++)for(let c=0;c<size;c++){const alpha=Math.min(a[r*size+c]*gain,MAX_ALPHA);if(alpha<ALPHA_THRESHOLD)continue;const i=((y+r)*data.width+x+c)*4,inv=1-alpha;for(let ch=0;ch<3;ch++)data.data[i+ch]=clamp(Math.round((data.data[i+ch]-alpha*LOGO_VALUE)/inv),0,255);}return data;}
export async function processGeminiImage(image:HTMLImageElement,settings:WatermarkSettings={}){const c=document.createElement('canvas');c.width=image.naturalWidth||image.width;c.height=image.naturalHeight||image.height;const x=c.getContext('2d',{willReadFrequently:true});if(!x)throw new Error('Canvas processing is unavailable.');x.drawImage(image,0,0,c.width,c.height);const d=x.getImageData(0,0,c.width,c.height),det=await detectGeminiWatermark(d,c.width,c.height,'image');if(det.matchFound){await removeGeminiWatermark(d,det,settings);x.putImageData(d,0,0);}return{canvas:c,detection:det};}
export async function createWatermarkEngine():Promise<WatermarkEngine>{return{getWatermarkInfo(width,height){const i=getWatermarkInfo(width,height);return{size:i.size,position:{x:i.x,y:i.y,width:i.width,height:i.height}};},async getAlphaMap(size){return alphaMap(Math.max(1,Math.round(size)));},async removeWatermarkFromImage(canvas,settings={}){const x=canvas.getContext('2d',{willReadFrequently:true});if(!x)throw new Error('Canvas processing is unavailable.');const d=x.getImageData(0,0,canvas.width,canvas.height),det=await detectGeminiWatermark(d,canvas.width,canvas.height,'image');if(det.matchFound){await removeGeminiWatermark(d,det,settings);x.putImageData(d,0,0);}return canvas;}};}
